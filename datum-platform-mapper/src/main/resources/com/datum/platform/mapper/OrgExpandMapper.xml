<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datum.platform.mapper.OrgExpandMapper">
    <select id="getUsableIdentityIdList" resultType="java.lang.String">
        select o.identity_id
        from dc_org o inner join mo_org_expand oe on o.identity_id = oe.identity_id
        where o.status = 1;
    </select>

    <select id="list" resultType="com.datum.platform.mapper.domain.OrgExpand">
        select o.identity_id,
               o.node_name,
               o.image_url,
               oe.authority_join_time
        from dc_org o inner join mo_org_expand oe on o.identity_id = oe.identity_id
        where o.status = 1 and oe.is_authority = 1;
    </select>

    <select id="countOfAuthority" resultType="java.lang.Integer">
        select count(1)
        from dc_org o inner join mo_org_expand oe on o.identity_id = oe.identity_id
        where o.status = 1 and oe.is_authority = 1;
    </select>

</mapper>
